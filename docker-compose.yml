services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3000:3000'
        env_file:
            - .env.prod

        depends_on:
            - db

    db:
        image: postgres:16
        ports:
            - '5434:5432'
        env_file:
            - .env.prod
